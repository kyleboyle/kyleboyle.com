box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: "HEAD"
    - kyleboyle/html-proofer-test:
        basedir: public
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: kyleboyle.com
        basedir: public
