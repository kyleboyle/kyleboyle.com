box: wercker/ruby
build:
  steps:
    - arjen/hugo-build:
        version: 0.12
    - kyleboyle/html-proofer-test
        basedir: public
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: kyleboyle.com
        basedir: public
