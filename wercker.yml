box: ruby
build:
  steps:
    - arjen/hugo-build
    - kyleboyle/html-proofer-test:
        basedir: public
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: kyleboyle.com
        basedir: public