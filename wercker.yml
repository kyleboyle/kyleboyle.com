box: ruby
build:
  steps:
    - arjen/hugo-build
    - kyleboyle/html-proofer-test:
        basedir: public
deploy:
  steps:
steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: kyleboyle.com
        basedir: public