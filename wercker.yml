box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.12
deploy:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: kyleboyle.com
        basedir: public
